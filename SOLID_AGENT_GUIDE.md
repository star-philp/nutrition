# 🏛️ SOLID 에이전트 가이드라인 (SOLID_AGENT_GUIDE.md)

이 문서는 본 프로젝트의 소스 코드를 수정하거나 기능을 추가할 때 AI 에이전트가 반드시 준수해야 하는 아키텍처 원칙과 코딩 규칙을 정의합니다.

## 1. SOLID 원칙 적용 지침

### **S: 단일 책임 원칙 (Single Responsibility Principle)**
- 하나의 클래스/모듈은 하나의 기능적 책임만 가져야 합니다.
- 예: `AnalysisService`는 분석 로직만 담당하며, 파일 저장이나 DB 직접 접근은 각각 `FileStorage`, `Repository` 레이어에 위임합니다.

### **O: 개방-폐쇄 원칙 (Open-Closed Principle)**
- 기존 코드를 수정하지 않고도 기능을 확장할 수 있어야 합니다.
- 새로운 분석기(예: PDF 전용 분석기)를 추가할 때 기존 `AnalysisService` 코드를 고치는 대신, 인터페이스를 상속받아 새로운 클래스를 추가합니다.

### **L: 리스코프 치환 원칙 (Liskov Substitution Principle)**
- 자식 클래스는 언제나 부모 클래스를 대체할 수 있어야 합니다.
- 모든 분석 엔진은 동일한 `analyze()` 메서드 시그니처를 유지해야 합니다.

### **I: 인터페이스 분리 원칙 (Interface Segregation Principle)**
- 클라이언트는 자신이 사용하지 않는 메서드에 의존하도록 강제되어서는 안 됩니다.
- 분석 엔진 인터페이스는 꼭 필요한 기능만 포함하며, 특정 엔진에만 필요한 기능은 별도 인터페이스로 분리합니다.

### **D: 의존성 역전 원칙 (Dependency Inversion Principle)**
- 고수준 모듈은 저수준 모듈에 의존해서는 안 되며, 둘 다 추상화에 의존해야 합니다.
- 컨트롤러나 서비스는 구체적인 클래스(`OpenAIAnalyzer`) 대신 인터페이스(`IAnalyzer`)에 의존합니다.

## 2. 분석 워크플로우 표준 (Standard Workflow)

본 시스템의 분석 프로세스는 항상 다음의 단계를 따릅니다:
1.  **Registration (등록)**: 파일을 시스템에 업로드하고 고유 ID와 초기 상태(`PENDING`)를 부여받습니다.
2.  **Processing (처리)**: 지정된 `IAnalyzer`를 통해 비동기 혹은 순차적으로 데이터를 처리합니다. 상태는 `PROCESSING`으로 변경됩니다.
3.  **Result (결과)**: 처리가 완료되면 상태를 `COMPLETED` 또는 `FAILED`로 변경하고 분석 결과 리포트를 저장합니다.

## 3. 에이전트 행동 강령
- 모든 새로운 서비스 로직은 `app/services/` 폴더에 위치시킵니다.
- 복잡한 비즈니스 로직은 `App` 클래스나 `Route` 핸들러에 직접 작성하지 않고 서비스 레이어로 분리합니다.
- 코드를 수정하기 전, 이 가이드를 다시 한번 읽고 설계가 SOLID 원칙에 부합하는지 자가 검증합니다.
